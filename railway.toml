[build]
builder = "DOCKERFILE"
dockerfilePath = "/Dockerfile"

[deploy]
startCommand = "gunicorn -w 1 -k uvicorn.workers.UvicornWorker app.main:app --bind 0.0.0.0:8000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
